# 受信任发布公钥

> 对应英文原文：[`README.md`](README.md)

此目录保存更新器信任的发布公钥。安装包清单必须由对应私钥签名，机器人才能接受。

## 添加轮换密钥

1. 先用旧密钥发布一个同时信任新公钥的版本。
2. 等设备获得新公钥后，再开始用新私钥签名。

不能先用新私钥发布，再期望旧设备认识它。删除旧公钥同样要等仍依赖旧签名的版本退出支持范围。私钥只存在于受保护的 CI 或本地密钥库。

