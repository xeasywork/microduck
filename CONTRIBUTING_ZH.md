# 贡献指南

> 对应英文原文：[`CONTRIBUTING.md`](CONTRIBUTING.md)

## 构建和测试

- 工作区使用 Rust/Cargo；提交前运行相关 crate 测试，跨模块修改应运行工作区测试。
- 板卡相关功能尽量先用纯逻辑测试、假 socket 或测试夹具验证，再到真机验收。
- `xtask` 负责发布、打包和跨平台构建中的仓库级任务。
- 需要系统库的 `mediad` 与板卡构建应遵循仓库已有 sysroot 和容器流程，不要绕开固定版本。

## 仓库布局

`robotd` 是控制循环；`duck-control` 提供硬件模型、观测、ONNX 和安全；`duck-ipc-proto` 定义 RPC；外围服务为 `btd`、`configd`、`padd`、`mediad`、`tofd`；`updater` 是更新引擎；`robotctl` 和 `duckctl` 是命令行工具。

## 约定

- 一个机制只由一份设计文档负责，其他文档通过链接引用。
- 安全关键路径使用明确类型、固定尺寸和启动期检查。
- 控制循环不能等待网络、媒体或慢速传感器。
- 修改 API 时同步更新协议版本、客户端、测试和文档。
- 修改安装内容时同时检查首次安装、升级、回滚和重启顺序。

## README 媒体

大型图片和视频使用 GitHub 附件链接，不直接提交大媒体文件。新增素材应说明动作、硬件和版本来源。

## 发布

发布由 CI、签名密钥和受保护环境完成。版本、清单、签名、安装钩子和健康检查必须一致，不要手工拼装无法验证的发布包。

